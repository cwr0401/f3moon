/** GameZone — mirrors zone.GameZone */
export interface GameZone {
  id: string
  name: string
  description: string
  max_rooms: number
  room_count: number
  created_at: string
  updated_at: string
}
